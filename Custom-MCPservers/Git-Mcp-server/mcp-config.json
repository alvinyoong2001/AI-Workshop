{
  "mcpServers": {
    "git-mcp-server": {
      "command": "node",
      "args": ["server.js"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}",
        "BITBUCKET_TOKEN": "${BITBUCKET_TOKEN}",
        "BITBUCKET_USERNAME": "${BITBUCKET_USERNAME}"
      }
    }
  }
} 